services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dual-kepzes-backend
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
